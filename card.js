class Card {
    constructor(imageSource, cardTitle, cardDescription, cardDiscount, cardOriginal, cardDiscounted, cardClassName) {
      this.imageSource = imageSource;
      this.cardTitle = cardTitle;
      this.cardDescription = cardDescription;
      this.cardDiscount = cardDiscount;
      this.cardOriginal = cardOriginal;
      this.cardDiscounted = cardDiscounted;
      this.cardClassName = cardClassName;
    }
  }

export default Card;